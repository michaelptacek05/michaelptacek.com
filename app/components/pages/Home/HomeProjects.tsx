import { ContentContainer } from "../../ContentContainer";
import { SectionHeader } from "../../SectionHeader";

export function HomeProjects() {
    return(
        <section>
            <ContentContainer>
                <SectionHeader heading="Projects" text="These are projects made by me" />
            </ContentContainer>
        </section>
    )
}