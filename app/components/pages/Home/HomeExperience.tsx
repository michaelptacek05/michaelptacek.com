import { Section } from "lucide-react";
import { ContentContainer } from "../../ContentContainer";
import { SectionHeader } from "../../SectionHeader";

export default function HomeExpecience() {
    return(
        <section>
            <ContentContainer>
                <SectionHeader heading="Experience" text="My professional journey and the companies I've worked with:" />
                
            </ContentContainer>
        </section>
    )
}